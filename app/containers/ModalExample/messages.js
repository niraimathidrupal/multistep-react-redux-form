/*
 * ModalExample Messages
 *
 * This contains all the text for the ModalExample container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ModalExample';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ModalExample container!',
  },
});
