/*
 * UserCourses Messages
 *
 * This contains all the text for the UserCourses container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserCourses';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserCourses container!',
  },
});
